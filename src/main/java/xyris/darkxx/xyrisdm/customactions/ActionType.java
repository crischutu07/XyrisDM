package xyris.darkxx.xyrisdm.customactions;

public enum ActionType {
   CONSOLE_COMMAND,
   ATTACKER_COMMAND,
   VICTIM_COMMAND;

   // $FF: synthetic method
   private static ActionType[] $values() {
      return new ActionType[]{CONSOLE_COMMAND, ATTACKER_COMMAND, VICTIM_COMMAND};
   }
}
